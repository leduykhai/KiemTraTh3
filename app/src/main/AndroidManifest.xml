<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.duykhai.kiemtrath3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KiemTraTH3">
        <activity android:name=".profile"></activity>
        <activity android:name=".ThongBao" />
        <activity android:name=".TrangChu" />
        <activity android:name=".ThietBi" />
        <activity android:name=".TaiKhoan" />
        <activity android:name=".Login" />
        <activity android:name=".GuiPhanHoi" />
        <activity android:name=".DonHang" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>